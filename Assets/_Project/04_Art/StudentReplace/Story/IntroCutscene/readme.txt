인트로 컷신(타이틀 다음, 스테이지 시작 전에 나오는 이야기 장면) 리소스 넣는 곳

파일 목록:
  cutscene01.png, cutscene02.png, cutscene03.png ...   장면 이미지들 - 각 1920x1080px
  button_next.png / button_prev.png / button_skip.png   다음/이전/건너뛰기 버튼 - 각 214x87px

슬라이드(장면) 수는 자유롭게 늘리거나 줄여도 됩니다 - 파일 넣고 나서
  "Tools > Class Template > Refresh Intro Cutscene Slides" 실행
번호는 두 자리로 순서대로 (01, 02 ... 09, 10, 11 ...)

정지 이미지 대신 동영상도 넣을 수 있습니다 - 같은 번호 자리에 확장자만 .mp4로:
  예) cutscene04.mp4   (이 번호의 장면이 정지 이미지 대신 영상으로 재생됨)
  - 파일명이 정확히 "cutscene"으로 시작해야 인식됩니다 (앞에 다른 글자를 붙이면 인식 안 되고 조용히 무시됨)
  - 권장 규격: H.264 코덱, 1920x1080 (이 규격이 아니어도 재생은 되고 화면 비율에 맞게 자동으로 레터박스 처리되지만, 화질을 위해 권장)
  - 영상 자체에 소리가 없으면 무음으로 재생됩니다 (별도 BGM 파일을 원하면 06_Audio/BGM/IntroCutscene에 추가)

규칙 (이미지):
  - PNG, 알파 채널 필요 없음(꽉 찬 배경 그림) - 1920x1080 고정
  - 버튼은 PNG, 알파 채널 투명, 214x87px 유지 (달라졌으면 Image 컴포넌트의 "Set Native Size"로 복구 가능)

넣고 나면 04_Unity_적용_가이드의 "그래픽 리소스 반영하기" 순서대로 진행하세요.
